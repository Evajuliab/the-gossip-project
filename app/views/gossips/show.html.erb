<h3>Voici la page du gossip n° <%= params['id'] %> </h3>

</br>
<div class="card">
  <div class="card-header">
  Titre : <%= @gossip.title%>
  </div>
  <div class="card-body">
    <blockquote class="blockquote mb-0">
      <p>Auteur :  <%= @gossip.user.try(:first_name)%></p>
      <blockquote class="blockquote mb-1">
      <%= @gossip.content %>.</p>
      <footer class="blockquote-footer">Auteur :  <%= @gossip.user.try(:first_name)%> </br>
      Créé le : <%= @gossip.created_at %></br>
    </blockquote>
  </div>
</div>


<button type="button" class="btn btn-outline-secondary"><%= link_to "En savoir plus sur l'auteur", user_path(@gossip.user) %></button>



<button type="button" class="btn btn-outline-secondary"><%= link_to "Editer mon gossip", edit_gossip_path %></button>


<%= button_to "Supprimer ce gossip", gossip_path(@gossip), method: :delete, class: "btn btn-danger"%><br/><br/>




<%= @gossip.comments.count %> commentaire(s)

<%= render @gossip.comments %>

<h3> Rajoute un commentaire : </h3>

<%= render "comments/create" %>


